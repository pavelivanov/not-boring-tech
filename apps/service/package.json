{
  "name": "@findthatproject/service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "sync": "tsx src/sync.ts",
    "digest": "tsx src/digest.ts",
    "eval:extraction": "tsx src/eval/run-extraction-eval.ts",
    "lint": "prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.12",
    "@findthatproject/contracts": "*",
    "@findthatproject/db": "*",
    "hono": "^4",
    "openai": "^6",
    "telegram": "^2",
    "zod": "^4"
  },
  "devDependencies": {
    "@findthatproject/typescript-config": "*",
    "@types/node": "^22",
    "prettier": "^3.8.3",
    "tsup": "^8",
    "tsx": "^4",
    "typescript": "^6",
    "vitest": "^4.1.6"
  }
}
