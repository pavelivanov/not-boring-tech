# Applications

Deployable FindThatProject applications live in this directory. Each application owns
its runtime dependencies, development dependencies, and task scripts.

Plan 002 will add the website, and Plan 003 will add the collector service.
