Commit all pending changes as a single conventional commit:

1. `git add -A` (includes the two new migration directories `20260831120000_separate_digest_cover` and `20260831130000_digest_kinds`).
2. Commit with message:

   `feat(digest): send cover photo and group items by content type`

   Body summarizing:
   - Send `weekly-digest-cover.png` once per channel as a standalone `sendPhoto` delivery (new `WeeklyDigestDeliveryKind` TEXT|PHOTO, `mediaUrl` column) before the text parts, which now have link previews disabled.
   - Group digest items under localized content-type headings in the website's canonical order, renumbered per display order; snapshot `kind` on `WeeklyDigestItem`.
   - Move kind/category label maps into `@findthatproject/contracts` (`TECHNOLOGY_KIND_LABELS`, `CATALOG_CATEGORY_LABELS`) and reuse in web and digest renderer.
   - Update renderer/coordinator/bot-client, tests, README, and plan doc; add Prisma migrations.

No push unless requested.